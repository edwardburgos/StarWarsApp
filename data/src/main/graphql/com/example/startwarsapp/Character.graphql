query Character( $characterId: ID!) {
    person (id: $characterId ) {
        id
        eyeColor
        hairColor
        skinColor
        birthYear
        vehicleConnection {
            vehicles {
                name
            }
        }
    }
}