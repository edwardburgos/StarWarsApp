query Character( $characterId: ID!) {
    person (id: $characterId ) {
        id
        name
        eyeColor
        hairColor
        skinColor
        birthYear
        vehicleConnection {
            vehicles {
                name
            }
        }
    }
}